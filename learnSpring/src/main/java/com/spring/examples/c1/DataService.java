package com.spring.examples.c1;

public interface DataService {
    // in interface of java we can have only two methods with body default and static
    // all the methods are abstract by default in java
    int [] retriveData(); // because in interface we can't have method with body
}
